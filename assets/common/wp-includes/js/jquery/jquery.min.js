/*! PCA static-site shim: jQuery is not required by the static pages. */
(function (window) {
  function JQueryShim() {
    return JQueryShim.fn;
  }

  JQueryShim.fn = {
    length: 0,
    ready: function (callback) {
      if (typeof callback === 'function') {
        if (document.readyState === 'loading') {
          document.addEventListener('DOMContentLoaded', callback);
        } else {
          callback();
        }
      }
      return this;
    },
    on: function () { return this; },
    off: function () { return this; },
    each: function () { return this; },
    addClass: function () { return this; },
    removeClass: function () { return this; },
    toggleClass: function () { return this; }
  };

  JQueryShim.fn.init = JQueryShim;
  JQueryShim.fn.init.prototype = JQueryShim.fn;
  JQueryShim.extend = function () { return JQueryShim; };
  JQueryShim.noop = function () {};

  window.jQuery = window.jQuery || JQueryShim;
  window.$ = window.$ || window.jQuery;
})(window);
